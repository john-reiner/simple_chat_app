<%= turbo_frame_tag dom_id(message) do %>
    <div class="cont-<%= message.user.id %>">
        <div class="user-message msg-<%= message.user.id %>">
            <p><%= message.content %></p>
        </div>
    </div>
<% end %>